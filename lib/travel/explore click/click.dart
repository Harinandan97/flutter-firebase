import 'package:flutter/material.dart';

class clickex extends StatefulWidget {
  const clickex({super.key});

  @override
  State<clickex> createState() => _clickexState();
}

class _clickexState extends State<clickex> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: [Text("data")],),
    );
  }
}
